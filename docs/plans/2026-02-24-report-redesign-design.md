# Report System Redesign — Design Document

**Date**: 2026-02-24
**Status**: Approved
**Scope**: LLM prompt, data model, frontend report UI, evidence system

---

## 1. 问题陈述

当前报告系统存在 4 个核心问题：

1. **视觉呈现不直观**：`[e_000921]` 式 evidence ID 直接暴露在 Team Summary 和 claim 文本中
2. **Evidence 内容混乱**：面试官非评价性发言（"That's correct"）占据 evidence，权重分配不合理
3. **缺少独立 Transcript 板块**：所有内容堆在 Evidence 页面，信息密度过高
4. **报告无差异化**：固定 5 维度 × 固定 1S/1R/1A = 正五边形雷达图，无法体现候选人特点

---

## 2. 设计决策摘要

| 决策点 | 选择 |
|--------|------|
| 维度评分机制 | 核心维度 + LLM 0-10 数值评分 |
| Evidence 引用格式 | 脚注式（上标数字 + 底部引用详情） |
| Evidence 来源权重 | 四级加权（面试者发言 > Memo > 面试官评价性发言 > 排除非评价性） |
| Transcript 布局 | Split View 侧边栏，与脚注双向联动 |
| 维度集合 | 混合方案：面试类型预设 + LLM 可建议调整 |

---

## 3. 数据模型变更

### 3.1 DimensionFeedback 重构

```typescript
// 旧结构
interface DimensionFeedback {
  dimension: "leadership" | "collaboration" | "logic" | "structure" | "initiative";
  strengths: DimensionClaim[];
  risks: DimensionClaim[];
  actions: DimensionClaim[];
}

// 新结构
interface DimensionFeedback {
  dimension: string;                // 自由字符串，不再是 5 值 enum
  label_zh: string;                 // 中文显示名，如 "逻辑推理"
  score: number;                    // 0-10 分，LLM 直接给出
  score_rationale: string;          // 简短评分理由（1-2 句）
  evidence_insufficient?: boolean;  // 证据不足以评估时为 true
  not_applicable?: boolean;         // LLM 判定此维度不适用
  strengths: DimensionClaim[];      // 0-5 个，按实际表现分配
  risks: DimensionClaim[];          // 0-3 个
  actions: DimensionClaim[];        // 0-3 个
}
```

### 3.2 DimensionClaim 文本净化

```typescript
interface DimensionClaim {
  claim_id: string;
  text: string;                     // 纯自然语言，不包含 [e_XXXXX] 引用
  evidence_refs: string[];          // 内部 ID 数组（前端渲染为脚注上标）
  confidence: number;
  supporting_utterances?: string[];
}
```

### 3.3 维度预设模板（新增）

```typescript
interface DimensionPresetItem {
  key: string;                      // "logical_reasoning"
  label_zh: string;                 // "逻辑推理"
  label_en: string;                 // "Logical Reasoning"
  description: string;              // LLM 评估指引
  weight: number;                   // 默认 1.0
}

interface DimensionPresetTemplate {
  interview_type: string;           // "academic" | "technical" | "behavioral" | "group"
  label_zh: string;                 // "学术面试"
  dimensions: DimensionPresetItem[];
}
```

**预设模板：**

| 面试类型 | 维度 |
|----------|------|
| 学术面试 | 学术动机、专业知识、逻辑推理、表达结构、主动性 |
| 技术面试 | 问题分析、代码能力、系统设计、沟通表达、主动性 |
| 行为面试 | 领导力、协作能力、抗压能力、自我认知、主动性 |
| 小组面试 | 领导力、协作能力、逻辑推理、表达结构、主动性 |

### 3.4 LLM 维度建议（新增）

```typescript
interface SuggestedDimension {
  key: string;
  label_zh: string;
  reason: string;                   // 建议理由
  action: "add" | "replace" | "mark_not_applicable";
  replaces?: string;                // 替换哪个维度的 key
}
```

### 3.5 Evidence Source Tier（新增）

```typescript
interface EvidenceItem {
  // ... 现有字段保持 ...
  source_tier: 1 | 2 | 3;          // 新增：来源级别
  source_label?: string;            // "面试者发言" | "面试官观察" | "辅助佐证"
}
```

### 3.6 Session Config 扩展

```typescript
// session_config 中新增
interface SessionContextMeta {
  mode: "1v1" | "group";
  interviewer_name?: string;
  position_title?: string;
  company_name?: string;
  interview_type?: string;          // 新增："academic" | "technical" | "behavioral" | "group"
  dimension_presets?: DimensionPresetItem[];  // 新增：用户选择的维度
  stage_descriptions: StageDescription[];
}
```

### 3.7 Overall Summary 结构变化

```typescript
// 旧结构
interface OverallFeedback {
  summary_sections: Array<{
    topic: string;
    bullets: string[];              // 包含 [e_XXXXX] 引用
    evidence_ids: string[];
  }>;
  team_dynamics: { highlights: string[]; risks: string[]; };
}

// 新结构
interface OverallFeedback {
  narrative: string;                // 纯自然语言段落，紧扣面试主题
  narrative_evidence_refs: string[];// 段落中引用的 evidence IDs（前端渲染为脚注）
  key_findings: Array<{
    type: "strength" | "risk" | "observation";
    text: string;                   // 纯文本
    evidence_refs: string[];
  }>;
  suggested_dimensions?: SuggestedDimension[];
}
```

---

## 4. LLM Prompt 重构

### 4.1 Evidence 来源分级

在构建 `evidence_pack` 时，对每个 evidence item 标注 `source_tier`：

- **tier_1（核心）**：面试者自己的发言（stream_role="students"）
- **tier_2（补充）**：面试官写的 Memos
- **tier_3（辅助）**：面试官的评价性口头反馈（stream_role="teacher" 且包含评价词汇 + 长度 > 10 字）
- **排除**：面试官的非评价性发言（"好的下一题"、"That's correct"、"请继续"等）

### 4.2 Tier 3 预过滤逻辑（Worker 侧）

```
evaluative_keywords_zh = ["很好", "不错", "到位", "准确", "优秀", "需要改进", "不太对", "有进步"]
evaluative_keywords_en = ["excellent", "good point", "impressive", "well done", "needs improvement", "not quite"]

对于 stream_role="teacher" 的 utterance：
  if 包含 evaluative_keywords 且 长度 > 10 字 → tier_3（保留）
  else → 排除（不进入 evidence_pack）
```

### 4.3 评分指令

LLM System Prompt 关键规则变更：

```
旧规则 #5: "For each interviewee, evaluate ALL 5 dimensions"
新规则:
1. 使用 session_context.dimension_presets 作为评估框架（3-6 个维度）
2. 为每个维度给出 0-10 分数值评分：
   - 0-2: 严重不足
   - 3-4: 明显短板
   - 5-6: 基本达标
   - 7-8: 表现良好
   - 9-10: 表现优异
3. score_rationale 用 1-2 句话概括评分依据
4. strengths/risks/actions 数量不固定（根据实际表现分配）
5. 如果某维度证据不足 → score=5, evidence_insufficient=true
6. 如果某维度完全不适用 → not_applicable=true
7. LLM 可输出 suggested_dimensions 建议调整

旧规则 #1: "Every claim MUST cite 1-5 evidence references using evidence_id"
新增补充:
- claim.text 必须是纯自然语言，不包含任何 [e_XXXXX] 引用标记
- 引用关系通过 evidence_refs 数组表达
- 优先引用 tier_1 evidence，tier_3 仅作为辅助

旧规则 #14: "Generate ≥2 summary_sections"
替换为:
- 生成 overall.narrative（自然语言段落，紧扣 session_context.position_title 和面试阶段）
- narrative 不包含 [e_XXXXX]，引用通过 narrative_evidence_refs 关联
- 生成 ≥3 key_findings（每个有 type + text + evidence_refs）
```

### 4.4 面试主题锚定

Prompt 中强化 session_context 的使用：

```
你正在为以下面试生成评估报告：
- 面试类型: {interview_type}
- 目标职位/项目: {position_title} @ {company_name}
- 面试官: {interviewer_name}
- 面试阶段: {stages}

所有评价必须围绕候选人是否适合「{position_title}」展开。
不要给出泛泛的能力评估——每个 claim 都要与目标职位/项目的具体要求关联。
```

---

## 5. 前端报告布局

### 5.1 Split View 整体结构

```
┌──────────────────────────────────────────────────────────┐
│  Header: 面试标题 / 日期 / 时长 / SplitButton            │
├────────────────────────────────┬─────────────────────────┤
│ 主内容区 (scrollable)          │ Transcript 面板          │
│                                │ (collapsible)           │
│ [Section Nav]                  │ [搜索] [说话人筛选]      │
│ Summary | 人名 | Evidence      │                         │
│                                │ [Tim] 02:15             │
│ (当前 Section)                  │ 好，我们开始吧...       │
│ ...文本... ¹                   │                         │
│                                │ [魏] 02:20 ← 高亮       │
│ ─────────────                  │ 我曾经做过...            │
│ ¹ [02:20] 魏: "引用..."       │                         │
├────────────────────────────────┴─────────────────────────┤
│ Transcript 面板切换按钮                                    │
└──────────────────────────────────────────────────────────┘
```

### 5.2 交互规则

1. **脚注上标点击**：
   - 主内容区滚动到脚注详情
   - 右侧 Transcript 面板自动滚动到对应时间点并高亮
   - 如果面板未展开，自动展开

2. **Transcript 面板**：
   - 被引用的段落带有 accent 色左边框标记
   - 点击标记反向跳转到引用该段的 claim
   - 默认窄屏折叠、宽屏展开（breakpoint: 1024px）
   - 折叠时主内容区占满宽度
   - 展开比例约 60:40

3. **脚注编号**：
   - 同一 Section 内连续编号：¹ ² ³ ...
   - 切换 Section 时编号重置
   - 每条脚注：`[时间戳] 说话人: "引用原文"`（最多 80 字）
   - 点击脚注展开完整引用

### 5.3 雷达图改造

```
旧：5 轴固定，分数 = strengths_count / total_claims（0-1）
新：N 轴动态（3-6），分数 = LLM score（0-10）

视觉：
- 轴标签：中文维度名 + 分数（如 "逻辑推理 8.5"）
- 网格环：2.5 / 5.0 / 7.5 / 10
- 低分维度（<4）轴标签用警示色（--color-risk）
- not_applicable 维度用虚线轴 + 灰色标签
```

### 5.4 Team Summary 呈现

```
┌──────────────────────────────────────────┐
│ 面试概览                                  │
│ 2026-02-24 | 33分钟 | 学术面试            │
│ 目标: 帝国理工 BTv 项目                    │
│ 面试官: Tim | 面试者: 魏一心               │
├──────────────────────────────────────────┤
│ 综合评估                                  │
│                                          │
│ （自然语言段落，脚注上标引用）              │
│ ...展现了跨学科创新意愿 ¹...              │
│                                          │
│ ─────────────────                        │
│ ¹ [02:20] 魏: "用数学模型优化代餐配方..."  │
│ ² ...                                    │
└──────────────────────────────────────────┘
```

---

## 6. Evidence Section 重构

### 6.1 定位变化

从 "所有引用大杂烩" → "结构化证据档案"

### 6.2 证据卡片

每张卡片包含：
- **Tier 标签**：🟢 tier_1 面试者发言 / 🔵 tier_2 面试官观察 / ⚪ tier_3 辅助佐证
- **引用原文**
- **说话人 + 时间戳**（tier_1/3）或 **Memo 作者 + Stage**（tier_2）
- **被引用次数 + 关联维度**
- 点击可跳转到引用它的 claim 或 Transcript 对应位置

### 6.3 筛选与排序

- 筛选：[全部] [面试者发言] [Memo 观察] [辅助佐证] + 维度下拉 + 人员下拉
- 排序：[时间顺序] [引用次数] [关联维度]
- 默认：按时间顺序，tier_1 + tier_2 展示，tier_3 折叠

### 6.4 数量控制

- tier_1：最多 30 条（按引用次数 + confidence 排序取 top）
- tier_2：全部保留
- tier_3：最多 10 条
- 超出部分通过 "显示更多" 加载

---

## 7. Setup 阶段维度配置

### 7.1 UI

在 SetupView Step 2 新增：
- 面试类型选择（学术/技术/行为/小组/自定义）
- 选择后自动填充对应维度预设
- 可手动增删维度（3-6 个范围）
- 每个维度显示名称 + 简短描述

### 7.2 数据流

```
SetupView 维度选择
  → session_config.dimension_presets
  → POST /v1/sessions/{id}/config 存储到 DO
  → finalize 时传入 LLM prompt
  → LLM 按预设维度评估 + 输出 suggested_dimensions
  → 前端展示 AI 建议（接受/保持原维度）
  → 确认后记录到 session config，re-generate 使用确认后的维度
```

---

## 8. 向后兼容

- `DimensionFeedback.dimension` 从 enum 变为 string → 旧报告的 "leadership" 等值仍然合法
- 旧报告没有 `score` 字段 → 前端 fallback 到现有的 strengths_count/total 计算
- 旧报告 claim.text 包含 `[e_XXXXX]` → 前端渲染时用正则替换为脚注格式
- `overall` 旧结构（summary_sections）→ 前端检测并降级渲染为 bullet list

---

## 9. 不在范围内

- 维度历史趋势对比（跨 session）
- 自定义评分量表（0-5, A-F 等）
- 多语言维度名称切换
- Evidence 手动编辑/删除
